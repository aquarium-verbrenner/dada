okBtn.addEventListener("click",() => {
    console.log("Btn clicked")
});
});


Document.addEventListener ("DOMContentLoaded", ())

const convertLectureArrayTuHtml = (lectures) =>{
    const html = "";
    retrun html
}

const buildLectureInformation = ()
const ects = document




const calculateTotalEffort = (lectures) => {
    let sum = 0
    for (const l of lectures) {
        sum += l.effort;
    }
    return sum
}
